{
  "name": "cleancue",
  "version": "0.2.4",
  "private": true,
  "description": "DJ Library Manager",
  "author": "CommndrCody",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "test": "jest --passWithNoTests",
    "test:unit": "pnpm -r test",
    "test:ui": "pnpm --filter @cleancue/ui test",
    "test:e2e": "jest tests/",
    "test:ui-integration": "jest tests/ui-integration.test.js --verbose",
    "test:build": "./scripts/test-build.sh",
    "test:health": "./scripts/health-check.sh",
    "test:all": "pnpm run test:health && pnpm run test:unit && pnpm run test:ui && pnpm run test:build",
    "test:ui-complete": "./scripts/test-ui-complete.sh",
    "test:ui-interactive": "./scripts/test-ui-interactive.sh",
    "ci": "./scripts/local-ci.sh",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "cli": "pnpm --filter @cleancue/cli",
    "desktop": "pnpm --filter @cleancue/desktop"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.14.0",
    "@typescript-eslint/parser": "^6.14.0",
    "electron": "^38.1.2",
    "eslint": "^8.55.0",
    "jest": "^29.7.0",
    "prettier": "^3.1.0",
    "spectron": "^19.0.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "dependencies": {
    "essentia.js": "^0.1.3"
  }
}
