# Minimal requirements for core audio analysis (tempo, key, energy)
# This creates a smaller bundle for basic functionality

librosa>=0.10.1
numpy>=1.24.0
scipy>=1.10.0
soundfile>=0.12.1

# Optional for better performance but not essential
numba>=0.57.0

# Basic audio metadata
mutagen>=1.47.0